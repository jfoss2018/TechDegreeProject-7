extends layout.pug

block content
  article.grid-container.portfolio-intro
    div
      h1 My Portfolio
      p.lead.text-light
        | The following applications showcase my initial projects with Team Treehouse. Each project is primarily an application of client-side JavaScript.
        br
        br
        | Coming soon: An expanded portfolio including examples of server-side JavaScript and more!
  article.grid-container.portfolio-index
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      each project in projects
        .cell
          a(href=`/projects/${project.id}`)
            img.thumbnail(src= project.image_urls[0])
            h5= project.project_name
